# 开发记录（项目上下文归档）

> 更新日期：2026-09-15
> 用途：记录极客园项目（react-jike-pro）已完成的内容、实现约定、联调结论和待办，方便下次继续接上下文。
> 对应教程笔记：`/Users/a0000/Documents/web/资料/React/React 基础 - day07/02-MD笔记/`（04 发布文章、05 文章列表、06 编辑文章、07 项目打包）

## 一、项目速览

- 技术栈：React 19 + TypeScript + Vite + Redux Toolkit + React Router 7 + antd 6 + axios + ECharts 6 + Sass
- 常用命令：`npm run dev`（默认 5173）/ `npm run build`（tsc + vite build）/ `npm run lint`
- 接口地址：`https://geek.itheima.net/v1_0`，开发环境由 `vite.config.ts` 的 `server.proxy` 把 `/api` 转发过去（只在 dev 生效，生产要 Nginx 反代）
- 测试账号：手机号 `13911111111`，验证码 `246810`
- 约定：路径别名 `@` → `src`；token 存在 localStorage 的 `jike_token`；接口方法统一放 `src/apis/`，类型统一放 `src/interface/index.tsx`；注释写"为什么这么做"，不写"做了什么"
- 代码风格：函数组件 + hooks，4 空格缩进，无分号结尾

## 二、已完成模块

提交记录：`b1d3f86 feat: 首页数据概览、发布文章、文章列表三个模块`

### 1. 首页数据概览（路由 `/`）

相关文件：[src/pages/Home/index.tsx](src/pages/Home/index.tsx)、[src/pages/Home/index.scss](src/pages/Home/index.scss)、[src/apis/home.ts](src/apis/home.ts)、[src/hooks/useECharts.ts](src/hooks/useECharts.ts)

- 四个统计卡片（总文章数 / 总粉丝数 / 总获赞数 / 总关注数）+ 「文章发布趋势」折线图（近 30 天发布量，带渐变面积填充）。
- ECharts 按需引入：`echarts/core` + `echarts.use([LineChart, GridComponent, TooltipComponent, CanvasRenderer])`。**不注册就画不出来**，这是按需引入最容易踩的坑。
- `useECharts` 托管实例生命周期：挂载 init、option 变化 setOption、`ResizeObserver` 自适应尺寸、卸载 dispose。
- 两个必须注意的点：图表容器要先渲染再画（藏进 loading 分支会导致依赖为空的 init 永不执行）；option 必须 `useMemo`，否则每次渲染都会重绘。
- 数据目前是 [src/apis/home.ts](src/apis/home.ts) 里的模拟数据（最近 30 天动态生成日期 + 手写数量）。接真实接口只需把 `getStatistics` 函数体换成 `http.get('/statistics')`，页面不用改。
- ECharts 6 里 `grid.containLabel` 已废弃（控制台会告警），等价写法是 `outerBoundsMode: 'same' + outerBoundsContain: 'axisLabel'`。

### 2. 发布文章（路由 `/publish`）

相关文件：[src/pages/Publish/index.tsx](src/pages/Publish/index.tsx)、[src/pages/Publish/index.scss](src/pages/Publish/index.scss)、[src/apis/article.ts](src/apis/article.ts)

- 频道下拉：`GET /channels`。
- 富文本：用 `react-quill-new`（项目已装，支持 React 19；笔记里的 `react-quill` 不支持）。样式必须引 `react-quill-new/dist/quill.snow.css`。
- 封面：Radio 选单图(1)/三图(3)/无图(0) + Upload 上传。
  - 上传地址写 `/api/upload` 走代理，避免跨域。
  - **Upload 内部用 XMLHttpRequest，不走 axios 拦截器，token 要手动放进 `headers`**，否则 401。
  - 图片列表受控（`fileList`）+ `useRef` 当仓库：切单图只显示第一张，切回三图还能把之前的都拿回来。
  - CSS 选择器要注意：上传按钮 `.ant-upload-select` 是 `.ant-upload-list` 的兄弟节点，而且 antd 内置尺寸规则的选择器优先级更高，所以尺寸样式写成 `.ant-upload-wrapper.ant-upload-picture-card-wrapper` 下面并列两个选择器。
- 提交：`POST /mp/articles?draft=false`，body 为 `{ title, channel_id, content, type, cover: { type, images } }`，无图时 `images` 为空数组。
- 校验：封面类型和数量必须一致（单图 1 张、三图 3 张、无图 0 张）；有图片还在上传中时不让提交。
- 发布成功后清空表单，**同时要把 `imageType` 这个 state 一起复位**（只 `form.resetFields()` 会出现表单显示单图、state 还是三图的错位）。

### 3. 文章列表（路由 `/article`）

相关文件：[src/pages/Article/index.tsx](src/pages/Article/index.tsx)、[src/apis/article.ts](src/apis/article.ts)

- 筛选区：状态 Radio（`''` 全部 / `0` 草稿 / `2` 审核通过）、频道 Select、日期 RangePicker（`antd/es/date-picker/locale/zh_CN`）、筛选按钮。
- 列表：`GET /mp/articles`，参数 `page / per_page / status / channel_id / begin_pubdate / end_pubdate`，返回 `{ results, total_count }`。
- 查询参数和分页放在同一个 `params` 对象里当单一数据源，参数一变 effect 就重新请求；筛选/翻页/删除都只改它。筛选后强制回到第 1 页。
- 删除：`DELETE /mp/articles/:id` + Popconfirm 二次确认；当前页只剩一条时删完自动往前翻一页。
- 编辑按钮跳 `/publish?id=xxx`（回填逻辑属于笔记 06，尚未实现）。
- 细节：状态映射 `0 草稿-橙 / 1 审核中-蓝 / 2 审核通过-绿`；封面取 `cover.images[0]`，没有就用 `src/assets/error.png` 兜底；用 `ignore` 标记防止先发出的旧请求覆盖新请求。

### 4. 编辑文章（路由 `/publish?id=xxx`）

相关文件：[src/pages/Publish/index.tsx](src/pages/Publish/index.tsx)、[src/apis/article.ts](src/apis/article.ts)、[src/interface/index.tsx](src/interface/index.tsx)

- `src/apis/article.ts` 新增 `getArticleDetail`（`GET /mp/articles/:id`）和 `updateArticle`（`PUT /mp/articles/:id?draft=false`）。
- `src/interface/index.tsx` 新增 `ArticleDetail` 类型；注意详情接口的发布时间字段叫 `pub_date`（带下划线），和列表接口的 `pubdate` 不同。
- 回填：`useSearchParams` 拿 `id`，`form.setFieldsValue({ title, channel_id, content, type: cover.type })`；详情里的 `id / pub_date` 不是表单项，不要整包展开塞给表单。
- 封面回填：`setImageType(cover.type)` + `setImageList(cover.images.map(...))`。antd 受控 fileList 要求每个元素有 `uid`（用图片 url 即可）和 `status: 'done'`，只传 `{ url }` 会导致删除/预览行为异常。
- 提交时图片地址要兼容两种来源：编辑回填的旧图在 `item.url`，本次新上传的在 `item.response.data.url`（`formatImageUrl` 统一处理），否则编辑时不动封面会提交一堆空地址。
- 编辑模式详情加载中用 `Spin` 包住表单并禁用提交按钮，防止空表单覆盖原文；`detailLoading` 初始值直接根据 `articleId` 计算，避免在 effect 体内同步 `setState`（React hooks 新规则会报错）。
- 面包屑/按钮文案：有 id 显示「编辑文章 / 更新文章」，无 id 显示「发布文章」；编辑成功后 `navigate('/article')` 跳回列表，新增成功保持原有清空表单逻辑。
- 图片仓库 `cacheImageList` 在回填时也要同步，否则编辑页切换封面类型会把回填的图弄丢。

## 三、全局改动

- [src/main.tsx](src/main.tsx)：包了 `ConfigProvider locale={zhCN}`，并设置 `dayjs.locale('zh-cn')`。日期面板的月份/星期取自 dayjs 语言包，只引 antd 的 locale 会出现「2026年Sep / Su Mo Tu」这种半中文；表格空数据、分页等文案由 ConfigProvider 负责（否则是 No data）。
- [src/interface/index.tsx](src/interface/index.tsx)：新增 `StatisticsData`、`Channel`、`CoverType`、`PublishFormValue`、`PublishArticleParams`、`UploadResponse`、`Article`、`ArticleListParams`、`ArticleListResponse`、`ArticleFilterForm`。
- `package.json`：把 dayjs 提为显式依赖（RangePicker 的类型要用）。
- `README.md`：已实现功能补充了首页、发布/编辑文章、文章列表、界面中文化等条目。
- `vite.config.ts`：新增 `base: './'`（子目录部署资源不 404）和 `preview.proxy`（预览 dist 时接口链路同样可用），代理配置提取成 `proxyConfig` 复用。

## 四、联调记录（真实接口 + 测试账号）

- 发布文章：单图、三图各发过一篇并且成功，回查接口确认 `cover.type/images`、`channel_id`、`content`（编辑器产出的 `<p>…</p>`）都正确。
- 文章列表：共 9 条、每页 5 条共 2 页；状态筛选「草稿」1 条、「审核通过」8 条；频道=开发者资讯 5 条；日期区间 09-13~09-15 得到 3 条 —— 都和接口的 `total_count` 对上了。
- 删除：确认弹窗、取消逻辑正常；删掉一篇自建测试文章后总数 9 → 8。
- 演示后端上还留着我建的测试文章（如「封面状态同步验证」「React 19 发布文章联调测试」），需要时可以直接删。

### 本次（2026-09-15 分支调试）补充结论

- `npm run lint` 与 `npx tsc -b` 全部通过；`npm run dev` 启动后首页 200、`/api/channels` 代理正常、Publish / home 模块均能被 Vite 正常转换，无编译错误。
- 真实接口已用测试账号验证：`GET /mp/articles/:id` 返回 `data` 含 `id / title / channel_id / content / cover / pub_date`；`GET /statistics` 返回 `data: null`（因此首页保留本地模拟数据）。
- 项目里 `http.get<T>` 的约定：泛型 `T` 传「接口 `data` 的类型」，调用处 `res.data` 拿到的就是这层（响应拦截器剥掉 axios 外层后，类型层和运行时层恰好对齐）；不要把 `{ message, data }` 整包传进泛型，否则类型错位。

## 五、已知坑与后端行为

- 日期区间**起止传同一天**时接口返回 0 条（后端拿完整时间戳和日期字符串比较，`2026-09-14 20:04:31` > `2026-09-14`）。要跨天选，或把结束日期往后放一天。
- `status=1` 表示「审核中」，笔记的筛选选项只给了全部/草稿/审核通过三种。
- 演示后端是共用的测试账号，不要当成持久数据。
- 沙箱里 `npm run dev` 监听端口需要授权；5173 被占用时会自动换到 5174（换端口后 localStorage 的登录态不共享，需要重新登录）。

## 六、下一步待办

1. ~~笔记 06 编辑文章~~：已完成（见上文「编辑文章」小节），本次在分支 `codex/continue-dev-tasks` 上实现并通过 lint / tsc / dev server 调试。
2. ~~首页接真实接口~~：已验证并决定保留模拟数据。实测演示后端 `GET /statistics` 固定返回 `data: null`，为了保证首页统计卡片和 ECharts 趋势图正常展示，`getStatistics` 直接返回本地模拟数据；真实接口对接写法已留在 `src/apis/home.ts` 注释里，接真实环境时替换函数体即可。
3. ~~笔记 07 项目打包~~：配置检查已完成。`base: './'` 支持子目录部署，`preview.proxy` 让本地预览产物时接口同样可用；生产环境仍需 Nginx 反代 `/api` 并做 history 路由 fallback（README 已写明）。打包产物本次未重新生成（按需求暂不打包）。
4. ~~推送到 GitHub~~：已完成。本机 `github.com:443`（HTTPS）连不通，但 SSH 22 端口可用，所以生成了 `~/.ssh/id_ed25519` 并把公钥加到 GitHub，`origin` 地址也从 HTTPS 改成了 SSH（`git@github.com:mywx3951-ux/jike-pro.git`）。`main` 已快进到 `b1d3f86`，本次改动推到了分支 `codex/continue-dev-tasks`（`7d7ccf7`），可在 GitHub 上开 PR 合并。

## 七、下次怎么快速复现验证

1. `npm run dev`（需要端口授权），浏览器打开终端提示的地址。
2. 用测试账号登录，进「文章管理」核对：总数、分页、状态筛选（草稿应为 1 条）、删除弹窗、「创建文章」的封面三种模式。
3. 想直接对账接口：用测试账号登录拿 token，再 `curl` 带 `Authorization: Bearer <token>` 请求 `https://geek.itheima.net/v1_0/mp/articles?...`，拿 `total_count` 和页面上的「共查询到 N 条结果」比对。
